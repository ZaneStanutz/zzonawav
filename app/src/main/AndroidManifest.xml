<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.trioszs.zaneprojectapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ZaneProjectApp">
        <activity
            android:name=".MainActivityDownloadMusic"
            android:exported="true" />
        <activity
            android:name=".MainActivityCreateAccount"
            android:exported="true" />
        <activity
            android:name=".MainActivityVerifiedLogin"
            android:exported="true" />
        <activity
            android:name=".MainActivityLogin"
            android:exported="true" />
        <activity
            android:name=".FullscreenActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ZaneProjectApp.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>